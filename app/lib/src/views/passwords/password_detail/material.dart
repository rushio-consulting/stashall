import 'package:flutter/material.dart';

class MaterialPasswordDetail extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Password detail'),
      ),
      body: Text('password details'),
    );
  }
}
